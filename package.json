{
  "name": "me",
  "version": "1.0.0",
  "description": "portfolio",
  "author": "donald",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=master --cleanup"
  },
  "dependencies": {
    "animate.css": "^3.7.2",
    "fullpage-vue": "^2.0.0",
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
